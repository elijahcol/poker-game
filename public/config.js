// Set this to your backend URL when frontend is on Vercel and backend is on Render.
// Example: window.BACKEND_URL = "https://poker-game-xxxx.onrender.com";
// Leave empty ("") for local dev / single-server deploy (same origin).
window.BACKEND_URL = "";
